// E3 T3.1: Next.js edge middleware — session refresh + route protection
// Delegates to proxy.ts which holds the full implementation.
// Closes #20

export { proxy as middleware, config } from './proxy'
